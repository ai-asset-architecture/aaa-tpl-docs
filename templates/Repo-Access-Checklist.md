# Repo Access Checklist

- [ ] 有 repo 讀取權限（`gh repo view <org>/<repo>`）
- [ ] 有 PR 讀取權限（`gh pr list --repo <org>/<repo>`）
- [ ] 有 Issue 讀取權限（`gh issue list --repo <org>/<repo>`）
- [ ] 具備需要的寫入權限（若需 push 或開 PR）
