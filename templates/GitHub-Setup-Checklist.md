# GitHub CLI Setup Checklist

- [ ] `gh` 已安裝（`gh --version`）
- [ ] `gh auth login` 已完成
- [ ] `gh auth status` 顯示登入成功
- [ ] `git config --global user.name` 已設定
- [ ] `git config --global user.email` 已設定
- [ ] 能列出 org repos（`gh repo list ai-asset-architecture`）
