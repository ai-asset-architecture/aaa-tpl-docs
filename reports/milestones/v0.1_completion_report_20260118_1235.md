# v0.1 Completion Report (20260118_1235)

## Governance
- `.github/GOVERNANCE.md` establishes branch protection, teams, and bootstrap order baselines.
- PR template includes Asset Promotion checklist in `.github/PULL_REQUEST_TEMPLATE.md`.

## CI / Workflows
- `aaa-actions` provides reusable `lint/test/eval/release` and `asset-promotion` workflows.
- `eval.yml` installs `runner/requirements.txt` when present.

## Templates
- `aaa-tpl-docs` includes ACC/PP/.ai-context and PRD/ADR templates.
- `aaa-tpl-service` and `aaa-tpl-frontend` provide minimal skeletons and docs linking to `<org>-docs`.

## Tools / Skills
- `aaa-tools` CLI supports `sync skills` and `sync workflows` with working sync logic.
- Skills SSOT is deduped into `aaa-tools/skills/{common,codex,agent}`.
- Antigravity workflows SSOT lives at `aaa-tools/workflows/agent`.

## Evals
- `smoke` suite retained as baseline.
- Governance-related suites added: `readme_required`, `workflow_tag_refs`, `skills_structure`, `prompt_schema`.
- `prompt_schema` uses full JSON Schema validation (`jsonschema`) via `runner/run_repo_checks.py`.
- Runner dependencies recorded in `aaa-evals/runner/requirements.txt`.
- Promotion checklist added in `aaa-evals/ASSET_PROMOTION.md`.
