name: nightly-governance
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  nightly:
    uses: ai-asset-architecture/aaa-actions/.github/workflows/nightly-governance.yaml@v1.5.3
    with:
      org: ai-asset-architecture
