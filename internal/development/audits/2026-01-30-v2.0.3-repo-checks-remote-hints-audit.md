# v2.0.3 Remote Repo-Checks Hints (Phase 1) Validation

**Task**: Verify CLI runtime hint for post-init repo-checks (Phase 1 only)  
**Objective**: Ensure a non-blocking, standardized hint is shown after successful repo/init flows  
**Date**: 2026-01-30  
**Status**: ⏳ **PENDING**

---

## Executive Summary

This validation will confirm that a CLI success path emits a standardized Post-init DoD hint that includes `aaa init repo-checks --suite governance` and does not alter exit codes or repo-checks behavior.

**Validation Method**: Manual output verification (CLI) + spot-check of unchanged logic

---

## Evidence Checklist

- [ ] CLI success path shows "Post-init DoD" hint with `aaa init repo-checks --suite governance`.
- [ ] Hint is non-blocking; exit code unchanged.
- [ ] No changes to repo-checks logic or suite selection.

---

## Deferred Items (Not in Scope)

- MCP response hints (Phase 2)
- A2A event payload hints (Phase 3)

---

## Notes

- Attach command output and timestamps after implementation.
