# v2.1.34 Milestone Summary

- date: 2026-04-04
- version: `v2.1.34`
- title: `Topology-Aware Materialization and Bootstrap Mapping`

## Summary
`v2.1.34` 將 materialization / bootstrap mapping 補成 topology-aware placement runtime，固定 `materialization_mapping_by_topology`、`workflow_target_scope`、`codeowners_target_scope`、`mapping_completeness`，並明寫 placement 不等於 governance authority。

## Preserved Runtime Assets
- topology-aware materialization/bootstrap mapping runtime
- governance CLI binding
- canonical pass/fail fixtures
- remote workflow run evidence
