# v2.1.26 Milestone Summary

- date: 2026-04-04
- version: `v2.1.26`
- title: `Offering Package Materialization and Bootstrap Mapping`

## Summary
`v2.1.26` 將 offering package 從定義層推進到 pre-topology materialization runtime，固定 required repos、template sync、workflow enablement 與 governance asset mapping，並明寫 downstream executable phases 必須以 `v2.1.34` 的 topology-aware mapping 為 precedence。

## Preserved Runtime Assets
- offering package materialization and bootstrap mapping runtime
- governance CLI binding
- canonical pass/fail fixtures
- remote workflow run evidence
