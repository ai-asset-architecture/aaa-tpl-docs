# Implementation Plan: v2.0.2 Dashboard Visual Governance

## Goal Description
Harden the AAA dashboard for v2.0.2 to align with Audit-Immune governance, enforce Trust Boundary visualization, and ensure dashboard metrics render directly from ledger-derived sources without local computation.

## User Review Required
> [!IMPORTANT]
> - Evidence Bundle Compliance KPI added (5 Core Files integrity).
> - Trust Boundary repo grouping replaces flat inventory view.
> - Dashboard is rendering-only; metrics must map to ledger_export.jsonl metrics keys.
> - i18n axis labels and single-column chart layout required.

## Proposed Changes
### [aaa-docs]
#### [MODIFY] docs/dashboard/index.html
- Add Evidence Bundle Compliance KPI tile.
- Add Trust Boundary grouping block.
- Add axis labels on charts.
- Add data source attribution (ledger_export.jsonl → metrics.*).
- Expand repo inventory to v2.0 core repos.

#### [MODIFY] docs/dashboard/dashboard.js
- Add i18n strings for new KPI, boundary labels, axis labels, data source label.

#### [MODIFY] docs/dashboard/dashboard.css
- Convert charts to single-column layout.
- Add axis label styles.
- Add Trust Boundary chip styles.
- Add data source note styling.

## Triple-Summary Protocol (v2.0.2)
### 1. Strategic Plan (戰略計畫摘要)
Elevate the dashboard from compliance display to governance projection: highlight Trust Boundary layers, add evidence integrity KPI, and ensure all metrics are provenance-linked to ledger outputs.

### 2. Schema Evolution (結構演進摘要)
No new schema introduced; UI consumes existing ledger_export.jsonl metrics keys. All additional labels are presentation-only, with i18n mapping for ZH-TW/EN.

### 3. Component Architecture (組件架構摘要)
Dashboard components: KPI grid (add evidence KPI), trend charts (single column + axis labels), Trust Boundary panel (repo grouping), inventory table (expanded repos + boundary column), data source note.

## Verification Plan
### Automated Tests
- N/A (static dashboard render). Ensure lint-free HTML/CSS/JS.

### Manual Verification
- Verify i18n toggle swaps axis labels and KPI strings.
- Verify charts display in single column, each with X/Y axis labels.
- Verify Trust Boundary grouping content matches v2.0 repos.
- Verify data source note appears and states ledger_export.jsonl → metrics.*.
