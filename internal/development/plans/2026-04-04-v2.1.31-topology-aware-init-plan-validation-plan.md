# Step1 Plan: v2.1.31 Topology-Aware Init Plan Validation

- date: 2026-04-04
- version: v2.1.31
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
AAA docs/template 已具 hybrid-friendly signals，但 `aaa-tools/aaa/init_commands.py` 仍把 dedicated `.github` repo existence 視為 hard prerequisite；因此本版要把 init plan validation 改寫成 topology-aware precondition law。

## Objective
建立 `v2.1.31` 的 Step1 契約基線，讓 `aaa init` plan validation 成為 topology-aware。
本版屬 `github_governance_topology_support_program`；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1 ~ v2.1.19` 的 primary scope。

## Upstream Runtime Laws Consumed
- `v2.1.2`
- `v2.1.11`
- `v2.1.19`
- `v2.1.30`

## No Semantics Override
- 本版只處理 `aaa init` precondition 在不同 topology 下的可適用性。
- 不得改寫 `v2.1.11` 的 permission semantics。
- 不得把 init preflight 升格成新的 topology truth source。
- 不得反向改寫 `v2.1.30` 的 topology law。
- 本版 append 只澄清 init-boundary semantics 與 user-facing signaling。
- 不得重新定義 `hybrid` completion law 本體。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.31-topology-aware-init-plan-validation-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.31-topology-aware-init-plan-validation-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.31-topology-aware-init-plan-validation-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.31-step1-exit-checklist.md`
5. `internal/development/contracts/ops/topology-aware-init-plan-validation.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.31-topology-aware-init-plan-validation.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.31-topology-aware-init-plan-validation.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須 machine-checkable 定義 `required_structure_by_topology` 與 `github_repo_existence_required`。
2. `repo_local` 模式下，缺 dedicated `.github` repo 不得自動 fail。
3. `dedicated_repo` 模式下，`.github` repo 仍可被要求。
4. `hybrid` 模式必須要求雙邊 evidence。
5. init plan validation 只裁決 declared / planned structure compatibility，不產生 runtime detected topology verdict。
6. 本版不得把 init plan validation 寫成新的 topology truth source。
7. `structure pass` 不得被外推成 `topology completion pass`。
8. 本版 append 不得回頭改寫 `v2.1.30` / `v2.1.35` 的 hybrid law 分工。
9. `version_index.md` 已追加 `v2.1.31` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。

## Out of Scope (Step1)
- 本版只建立 init validation contract，不實作 CLI。
- 不處理 package definition、materialization 或 status runtime。
- 不修改任何 runtime domain code。

## Triple-Summary Protocol (v2.1.31)
### 1. Strategic Plan
先把 init preflight 從 dedicated-only 檢查提升為 topology-aware validation。

### 2. Schema Evolution
新增 `topology-aware-init-plan-validation.v0.1.schema.json`，固定 topology-aware init plan contract。

### 3. Component Architecture
Step1 僅交付 init validation bundle；Step2 若啟動，才把 bundle 對應到 `aaa-tools/aaa/init_commands.py`。

## Appendix Ref
本版只處理 init plan validation 的 topology-aware precondition，不建立新的 primary law。
