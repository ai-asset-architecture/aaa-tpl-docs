# Step1 Plan: v2.1.30 GitHub Governance Topology Contract Baseline

- date: 2026-04-04
- version: v2.1.30
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
AAA 的文件與模板層已存在 hybrid-friendly governance signals（org-level `.github` governance entry + repo-local `.github` structures），但 CLI / init / package runtime 仍將 dedicated `.github` repo existence 視為 hard prerequisite；因此本版的責任不是引入陌生概念，而是把既有隱含治理意圖升格為 machine-checkable runtime law。

## Objective
建立 `v2.1.30` 的 Step1 契約基線，將 GitHub governance topology 收斂為 machine-checkable contract baseline。
本版屬 `github_governance_topology_support_program`；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1 ~ v2.1.19` 的 primary scope。

## Upstream Runtime Laws Consumed
- `v2.1.1` Tool / Command Contract Baseline
- `v2.1.2` Multi-Repo / Worktree Identity Law
- `v2.1.3` Context Assembly / Current Truth Law
- `v2.1.19` Runtime Composition Root and System Assembly

## No Semantics Override
- 本版只將既有 hybrid-friendly governance intent 升格為 machine-checkable topology law。
- 不得改寫 `v2.1.2` 的 repo/worktree identity semantics。
- 不得改寫 `v2.1.3` 的 current truth / contamination semantics。
- 不得改寫 `v2.1.19` 的 composition root semantics。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.30-github-governance-topology-contract-baseline-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.30-github-governance-topology-contract-baseline-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.30-github-governance-topology-contract-baseline-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.30-step1-exit-checklist.md`
5. `internal/development/contracts/ops/github-governance-topology-contract-baseline.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.30-github-governance-topology-contract-baseline.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.30-github-governance-topology-contract-baseline.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須 machine-checkable 定義 `topology_mode`、`authority_split`、`asset_class_authority_model`、`precedence_law`、`conflict_law`、`evidence_sufficiency_law`。
2. `hybrid` 必須升級為 asset-class authority model，不得只停留在 enum。
3. 不得把 dedicated `.github` repo existence 繼續當作 topology 成立的唯一真相。
4. 本版必須明寫 `No Semantics Override`，不得改寫 `v2.1.2`、`v2.1.3`、`v2.1.19` 的 semantics。
5. fail example 必須表達至少一個 `hybrid` 無 authority model 的違規情形，以及至少一個 evidence insufficiency 卻仍宣稱 topology 成立的違規情形。
6. `version_index.md` 已追加 `v2.1.30` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。
7. `version_index.md` 的 `status` / `run_ref` / step wording 必須使用 canonical phrase，不得出現同義改寫。

## Out of Scope (Step1)
- 本版只建立 topology law，不實作 CLI / validator / runtime。
- 不處理 package resolution、materialization 或 status runtime。
- 不修改 `aaa-tools`、`aaa-docs`、`aaa-tpl-frontend` 或任何 runtime domain code。

## Triple-Summary Protocol (v2.1.30)
### 1. Strategic Plan
先把 topology / authority / precedence / conflict / evidence sufficiency law 定型，再讓後續 init、package、repo-check 成為 topology-aware。

### 2. Schema Evolution
新增 `github-governance-topology-contract-baseline.v0.1.schema.json`，固定三種 topology mode 與 hybrid asset-class authority model。

### 3. Component Architecture
Step1 僅交付 topology baseline bundle 與 pass/fail fixtures；Step2 若啟動，才把此 bundle 對應到 `aaa-tools` topology-aware runtime。

## Appendix Ref
本版是 `github_governance_topology_support_program` 的 law baseline。
本版只處理 topology contract，不建立新治理宇宙，也不得覆寫既有 core line semantics。
