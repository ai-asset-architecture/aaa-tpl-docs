# Step1 Plan: v2.1.24 Offering Package Definition Resolution

- date: 2026-04-04
- version: v2.1.24
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
`v2.1.32` 已定義 post-topology canonical definition resolution shape；因此 `v2.1.24` 只保留 pre-topology definition baseline。其責任是提供 mother-draft anchored resolution input，不再保有 dedicated `.github` repo 的 universal invariant 地位。

## Objective
建立 `v2.1.24` 的 Step1 契約基線，將 offering definition 轉成 machine-checkable resolution bundle。
本版屬 `offering_package_runtime_enablement_line` 的 pre-topology baseline；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1` 到 `v2.1.23` 的 primary scope；本版把人類治理母稿視為 source definition artifact，runtime 只做 resolution，不得 back-write 或 reinterpret mother draft；後續 executable phases 必須以 `v2.1.32` 的 topology-aware resolution 為優先。

## Upstream Runtime Laws Consumed
- `v2.1.23`
- `v2.1.30`
- `v2.1.32`

## No Semantics Override
- 本版只處理 pre-topology definition baseline。
- 不得 back-write 或 reinterpret mother draft。
- 不得把 dedicated `.github` repo 保留為 universal invariant。
- downstream executable phases 必須消費 `v2.1.32` 的 topology-aware resolution。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.24-offering-package-definition-resolution-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.24-offering-package-definition-resolution-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.24-offering-package-definition-resolution-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.24-step1-exit-checklist.md`
5. `internal/development/contracts/ops/offering-package-definition-resolution.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.24-offering-package-definition-resolution.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.24-offering-package-definition-resolution.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須以 machine-checkable 方式定義 package definition resolution，不允許 prose-only fallback。
2. schema 必須固定 `minimum_repo_set`、`workflow_inclusion_level`、`client_prerequisites` 的 shape，不得延後到後續版本。
3. source definition artifact 必須明確指向 `aaa-docs/bootstrap/offering_definition_skeleton.md`，runtime 不得 back-write、reinterpret 或反向奪權。
4. 本版必須明寫其為 pre-topology baseline，post-topology executable phases 必須以 `v2.1.32` resolved definition precedence 為準。
5. 本版不得外溢到 prerequisite gate、materialization、status 或 evidence judgment。
6. fail example 必須表達至少一個缺少 required shape，以及至少一個 runtime 嘗試改寫 source definition artifact 的違規情形。
7. 本版必須明確聲明其僅消費既有 contract，不得出現隱性 primary law 增殖。
8. `version_index.md` 已追加 `v2.1.24` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。
9. `version_index.md` 的 `status` / `run_ref` / step wording 必須使用 canonical phrase，不得出現同義改寫。

## Out of Scope (Step1)
- 本版只建立 Step1 runtime-plane baseline，不實作 runtime entry / validator / CLI wiring / frontend publish / workflow artifact。
- 不建立 prerequisite gate verdict 或 package activation 狀態。
- 不修改 `aaa-tools`、`aaa-docs`、`aaa-tpl-frontend`、`aaa-evals` 或任何 runtime domain code。

## Risks
- 若 source definition artifact 與 resolution bundle 的邊界不先固定，runtime 很容易反向奪取 offering layer。
- 若 `minimum_repo_set`、`workflow_inclusion_level`、`client_prerequisites` shape 不先硬化，後續 mapping 與 gate 會失去可驗證基礎。

## Triple-Summary Protocol (v2.1.24)
### 1. Strategic Plan (戰略計畫摘要)
把 offering definition 從人類治理母稿轉成可解析的 machine-checkable resolution bundle，作為 package runtime 線的共同語義輸入。

### 2. Schema Evolution (結構演進摘要)
新增 `offering-package-definition-resolution.v0.1.schema.json`，固定 source definition artifact、minimum repo set、workflow inclusion level 與 client prerequisites。

### 3. Component Architecture (組件架構摘要)
Step1 僅交付 definition resolution bundle 與 pass/fail fixtures；Step2 若啟動，才把此 bundle 對應到 `aaa-tools` package resolution runtime。

## Appendix Ref
本版屬 `offering_package_runtime_enablement_line`。
本版只處理 definition resolution，不建立新的 primary law，也不得 back-write 或 reinterpret mother draft。
