<template id="plan">
# Implementation Plan: v2.0.3 A2A Repo-Checks Hints (Phase 3)

## Goal Description
Define the A2A payload hint for repo creation/initialization so remote agents receive the mandatory `aaa init repo-checks --suite governance` instruction via A2A. This is a **spec placeholder only** until AAA has an A2A agent/channel available for verification.

## User Review Required
> [!IMPORTANT]
> 1) This plan is **Step 1 only** (spec/plan + audit stub). Implementation is deferred until A2A is available.
> 2) The A2A hint must be additive and non-blocking (no behavior change).

## Proposed Changes
### [aaa-tools]
#### [PLANNED] A2A response payload
- Add a standardized `post_init_required` (or `next_steps`) field containing `aaa init repo-checks --suite governance` to the A2A response for repo creation/initialization events.
- Preserve existing payload keys and semantics.

### [aaa-tpl-docs]
#### [NEW] internal/development/audits/2026-01-30-v2.0.3-a2a-repo-checks-hints-audit.md
- Record verification evidence once A2A transport is available; until then, keep status PENDING.

## Triple-Summary Protocol (v2.0.3)
### 1. Strategic Plan (戰略計畫摘要)
Reserve the A2A-level repo-checks hint so remote agents get mandatory post-init guidance via protocol-level payloads when A2A becomes available.

### 2. Schema Evolution (結構演進摘要)
No schema changes now; A2A payload will gain an additive hint field when implemented.

### 3. Component Architecture (組件架構摘要)
A2A response enrichment only; no changes to repo-checks execution or CLI/MCP behavior.

## Verification Plan
### Automated Tests
- Add A2A payload tests **after** A2A transport exists.

### Manual Verification
- Inspect A2A `repo.created` / `init.completed` response to confirm hint field presence.
</template>
