# Step1 Plan: v2.1.37 Public Package CLI Entry Surface

- date: 2026-04-05
- version: v2.1.37
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
`v2.1.23 ~ v2.1.29` 已建立 package runtime/application line，但 outside-in 驗證證明 remote client 仍缺少 public `aaa package ...` entry surface。

## Objective
建立 `v2.1.37` 的 Step1 契約基線，讓 package selection / resolution / status 對外有正式 CLI access surface。

## Upstream Runtime Laws Consumed
- `v2.1.23`
- `v2.1.24`
- `v2.1.27`
- `v2.1.30`
- `v2.1.35`
- `v2.1.36`

## No Semantics Override
- public package CLI 是既有 package/runtime truths 的 access surface。
- 不得重寫 mother-draft / package definition authority。
- 不得把 CLI surface 反向長成新的 truth center。
- 不得改寫 `v2.1.24` 的 source definition authority。
- public CLI entry surface 只覆蓋 package selection / resolution / status discovery。
- 不預先暗示 future mutation / activation / publish semantics。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-05-v2.1.37-public-package-cli-entry-surface-plan.md`
2. `internal/development/audits/2026-04-05-v2.1.37-public-package-cli-entry-surface-audit.md`
3. `internal/development/reviews/2026-04-05-v2.1.37-public-package-cli-entry-surface-diff-paths.md`
4. `internal/development/reviews/2026-04-05-v2.1.37-step1-exit-checklist.md`
5. `internal/development/contracts/ops/public-package-cli-entry-surface.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-05-v2.1.37-public-package-cli-entry-surface.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-05-v2.1.37-public-package-cli-entry-surface.fail.json`
8. `ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須 machine-checkable 固定公開 command family。
2. `package_level` 只接受 `lite | core | full` canonical enum。
3. public package CLI 必須被表達為 access surface，而不是新 authority source。
4. schema 必須顯式表達 `package_definition_authority_source` 與 `cli_is_authority_source=false`。
5. `version_index.md` 已追加 `v2.1.37` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。

## Out of Scope (Step1)
- 不實作 CLI commands。
- 不變更 package runtime code。
- 不改寫 package mother draft。

## Triple-Summary Protocol (v2.1.37)
### 1. Strategic Plan
把 package runtime 對外暴露成 public CLI entry surface，但維持既有 package truth authority 不變。

### 2. Schema Evolution
新增 `public-package-cli-entry-surface.v0.1.schema.json`，固定 public package CLI contract。

### 3. Component Architecture
Step1 僅交付 CLI access surface contract；Step2 若啟動，才對應到 `aaa-tools/aaa/cli.py` 與 package command family。

## Appendix Ref
本版只建立 public package CLI access surface，不建立新的 package law sovereignty。
