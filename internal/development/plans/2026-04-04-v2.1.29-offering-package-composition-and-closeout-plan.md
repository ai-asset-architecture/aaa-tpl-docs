# Step1 Plan: v2.1.29 Offering Package Composition and Closeout

- date: 2026-04-04
- version: v2.1.29
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
`v2.1.36` 已定義 topology support line 的 closeout 與 consumed-stage completeness；因此 `v2.1.29` 只保留 pre-topology package closeout baseline。後續 executable phases 必須消費 topology closeout 成果，不能再以 dedicated-only package baseline 宣稱 package runtime core complete。

## Objective
建立 `v2.1.29` 的 Step1 契約基線，將 offering package runtime core 收斂為單一 closeout composition。
本版屬 `offering_package_runtime_enablement_line` 的 pre-topology closeout baseline；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1` 到 `v2.1.28` 的 primary scope；本版只 close out package runtime core consumed stages，不重新裁決 offering semantics，也不得觸發 `v2.1.20 ~ v2.1.22`；後續 executable phases 必須消費 `v2.1.36` 的 topology closeout semantics。

## Upstream Runtime Laws Consumed
- `v2.1.23`
- `v2.1.24`
- `v2.1.25`
- `v2.1.26`
- `v2.1.27`
- `v2.1.28`
- `v2.1.30`
- `v2.1.36`

## No Semantics Override
- 本版只處理 pre-topology package closeout baseline。
- 不得重新裁決 topology semantics。
- 不得保留 dedicated-only implicit sovereignty。
- downstream executable phases 必須消費 `v2.1.36` 的 topology closeout。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.29-offering-package-composition-and-closeout-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.29-offering-package-composition-and-closeout-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.29-offering-package-composition-and-closeout-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.29-step1-exit-checklist.md`
5. `internal/development/contracts/ops/offering-package-composition-and-closeout.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.29-offering-package-composition-and-closeout.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.29-offering-package-composition-and-closeout.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須以 machine-checkable 方式定義 package runtime closeout composition，不允許 prose-only fallback。
2. consumed package runtime stages 必須以顯式可枚舉集合表示，不得以模糊總稱替代 completeness 聲明。
3. composition closeout 只可收斂 `v2.1.23 ~ v2.1.28` 的 package runtime core，不得重新裁決 offering semantics。
4. composition closeout 不得觸發或吸收 `v2.1.20 ~ v2.1.22` conditional expansion line。
5. 本版不得把 docs alignment、AI orchestration 或 external expansion 混入 closeout 必要條件。
6. fail example 必須表達至少一個 consumed stage 缺漏卻仍宣稱 closeout 完成，以及至少一個 closeout 反向奪權改寫 offering semantics 的違規情形。
7. 本版必須明確聲明其僅消費既有 contract，不得出現隱性 primary law 增殖。
8. `version_index.md` 已追加 `v2.1.29` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。
9. `version_index.md` 的 `status` / `run_ref` / step wording 必須使用 canonical phrase，不得出現同義改寫。

## Out of Scope (Step1)
- 本版只建立 Step1 runtime-plane baseline，不實作 runtime entry / validator / CLI wiring / frontend publish / workflow artifact。
- 不建立 AI orchestration、remote session、external resource integration 或 operator surface。
- 不修改 `aaa-tools`、`aaa-docs`、`aaa-tpl-frontend`、`aaa-evals` 或任何 runtime domain code。

## Risks
- 若 closeout composition 不先限制 consumed stages 與 semantics 邊界，`v2.1.29` 很容易變成新的 umbrella version。
- 若 conditional expansion 被帶入 closeout 必要條件，整條 package runtime 線會重新與 `v2.1.20 ~ v2.1.22` 混線。

## Triple-Summary Protocol (v2.1.29)
### 1. Strategic Plan (戰略計畫摘要)
把 package runtime 線的 selection、resolution、gate、mapping、status/evidence、machine-read-boundary 收斂成單一 closeout composition，形成 package runtime core。

### 2. Schema Evolution (結構演進摘要)
新增 `offering-package-composition-and-closeout.v0.1.schema.json`，固定 consumed stages、closeout boundary 與 non-expansion 限制。

### 3. Component Architecture (組件架構摘要)
Step1 僅交付 package closeout bundle 與 pass/fail fixtures；Step2 若啟動，才把此 bundle 對應到 `aaa-tools` package composition runtime。

## Appendix Ref
本版屬 `offering_package_runtime_enablement_line` closeout version。
本版只處理 package runtime core closeout，不建立新的 primary law，也不得重新裁決 offering semantics 或觸發 conditional expansion。
