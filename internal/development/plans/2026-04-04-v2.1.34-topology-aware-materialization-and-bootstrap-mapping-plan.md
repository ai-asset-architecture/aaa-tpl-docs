# Step1 Plan: v2.1.34 Topology-Aware Materialization and Bootstrap Mapping

- date: 2026-04-04
- version: v2.1.34
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
materialization / bootstrap mapping 目前仍容易隱含 dedicated `.github` placement；若不先 topology-aware，後續 package runtime 會長在錯的 governance placement 假設上。

## Objective
建立 `v2.1.34` 的 Step1 契約基線，讓 materialization / bootstrap mapping 支援 `dedicated_repo`、`repo_local`、`hybrid` 三種 topology。

## Upstream Runtime Laws Consumed
- `v2.1.26`
- `v2.1.30`
- `v2.1.32`
- `v2.1.33`

## No Semantics Override
- 本版只處理 topology-aware placement 與 bootstrap mapping。
- 不得改寫 `v2.1.26` 的 materialization/status boundary。
- materialization success 不得被推定為 package active。
- 不得以 topology mapping 反向裁決 package compliance semantics。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.34-topology-aware-materialization-and-bootstrap-mapping-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.34-topology-aware-materialization-and-bootstrap-mapping-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.34-topology-aware-materialization-and-bootstrap-mapping-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.34-step1-exit-checklist.md`
5. `internal/development/contracts/ops/topology-aware-materialization-and-bootstrap-mapping.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.34-topology-aware-materialization-and-bootstrap-mapping.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.34-topology-aware-materialization-and-bootstrap-mapping.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須 machine-checkable 定義 `materialization_mapping_by_topology`、`workflow_target_scope`、`codeowners_target_scope`、`mapping_completeness`。
2. materialization 不得預設 `.github` 一定是獨立 repo。
3. `repo_local` / `hybrid` 都必須有 placement rules。
4. materialization success 不得等於 package active。
5. `version_index.md` 已追加 `v2.1.34` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。

## Out of Scope (Step1)
- 不實作 materialization runtime。
- 不處理 status/compliance verdict。
- 不修改任何 runtime domain code。

## Triple-Summary Protocol (v2.1.34)
### 1. Strategic Plan
把 governance asset placement 變成 topology-aware mapping，避免 bootstrap 默認 dedicated-only。

### 2. Schema Evolution
新增 `topology-aware-materialization-and-bootstrap-mapping.v0.1.schema.json`，固定 topology-aware mapping shape。

### 3. Component Architecture
Step1 僅交付 mapping contract；Step2 若啟動，才對應到 CLI/bootstrap materialization。

## Appendix Ref
本版只處理 topology-aware materialization / bootstrap mapping，不建立新 primary law。
