# AAA v0.5 Complete Milestone Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add `ops/complete-milestone` runbook and governance rule so milestone completion auto-generates evidence, updates definition, reindexes, and notifies.

**Architecture:** Implement a runbook in `aaa-tools` and add a trigger rule in `aaa-tpl-docs/AI_COMMAND_CENTER.md`. Verify with runbook schema validation and reindex affected directories.

**Tech Stack:** JSON runbook, Markdown docs, aaa-tools CLI, aaa-evals schema validation.

### Task 1: Add runbook definition in aaa-tools

**Files:**
- Create: `aaa-tools/runbooks/ops/complete-milestone.yaml`

**Step 1: Write the failing test**

N/A (runbook is declarative).

**Step 2: Write minimal implementation**

Create runbook JSON with:
- Inputs: `milestone_id`, `approver`, `definition_path`
- Steps: verify → evidence → update definition → reindex → notify

**Step 3: Verify JSON schema**

Run: `python3 -m json.tool runbooks/ops/complete-milestone.yaml`

**Step 4: Commit**

```bash
git add runbooks/ops/complete-milestone.yaml
git commit -m "feat: add complete-milestone runbook"
```

### Task 2: Update AI command center policy

**Files:**
- Modify: `aaa-tpl-docs/AI_COMMAND_CENTER.md`

**Step 1: Write the change**

Add rule: when milestone verified PASS, agent must run `aaa run ops/complete-milestone` and notify humans with evidence links.

**Step 2: Commit**

```bash
git add AI_COMMAND_CENTER.md
git commit -m "docs: add milestone completion trigger rule"
```

### Task 3: Reindex affected directories

**Files:**
- Modify: `aaa-tools/specs/README.md`, `aaa-tools/specs/index.json` (if spec updates required)
- Modify: `aaa-tpl-docs/README.md` or other index if AI_COMMAND_CENTER is indexed

**Step 1: Run update-index where needed**

Use `python3 -m aaa.cli governance update-index` on directories that have index.json.

**Step 2: Commit**

```bash
git add <paths>
git commit -m "chore: refresh indexes after completion protocol"
```

### Task 4: Verify runbook schema

**Step 1: Run validation**

```bash
python3 runner/run_repo_checks.py --check runbook_schema_validate --repo /Users/imac/Documents/Code/AI-Lotto/AAA_WORKSPACE
```

