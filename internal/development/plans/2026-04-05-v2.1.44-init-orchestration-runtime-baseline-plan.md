# Step1 Plan: v2.1.44 Init Orchestration Runtime Baseline

- date: 2026-04-05
- version: v2.1.44
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
在 `v2.1.42` 收回足夠 intermediate bundle generation responsibility、且 `v2.1.43` 建立 CLI substrate 之前，supported path 仍主要是文件順序與手動串接；尚未形成 machine-executable init flow。

## Objective
建立 `v2.1.44` 的 Step1 契約基線，把 `select -> resolve -> validate-plan -> bundle generation -> gate -> mapping -> status` 鎖成可被 runtime 執行的 init orchestration sequence。

## Upstream Runtime Laws Consumed
- `v2.1.24`
- `v2.1.25`
- `v2.1.26`
- `v2.1.27`
- `v2.1.42`
- `v2.1.43`

## No Semantics Override
- 本版建立 orchestration runtime baseline，但不是 readiness certification。
- 不擴張多 public paths。
- 不新增 interactive shell UI。
- 不把 docs 順序誤寫成已完成 runtime sequence。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-05-v2.1.44-init-orchestration-runtime-baseline-plan.md`
2. `internal/development/audits/2026-04-05-v2.1.44-init-orchestration-runtime-baseline-audit.md`
3. `internal/development/reviews/2026-04-05-v2.1.44-init-orchestration-runtime-baseline-diff-paths.md`
4. `internal/development/reviews/2026-04-05-v2.1.44-step1-exit-checklist.md`
5. `internal/development/contracts/ops/init-orchestration-runtime-baseline.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-05-v2.1.44-init-orchestration-runtime-baseline.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-05-v2.1.44-init-orchestration-runtime-baseline.fail.json`
8. `ops/index/version_index.md`
9. `ops/index/workflow_index.md`

## Acceptance Criteria
1. orchestration step graph 必須 machine-readable 定義 step dependency、precondition、postcondition。
2. JSON contract 必須固定 failure/retry/downgrade semantics，不得只剩 prose。
3. JSON contract 必須含 emitted execution trace baseline。
4. 本版不得把 orchestration runtime 結果誤寫成 readiness certification verdict。
5. `version_index.md` 與 `workflow_index.md` 已新增 `v2.1.44` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。

## Step2 Scarcity Rationale
- 不能 bridge-only 的原因：orchestration runtime 若無 executable evidence，無法證明 step graph、retry law、trace output 真正可執行。
- 不能被既有 execution package 吸收的原因：既有 workflows 是分段 validator，不是 end-to-end init orchestration carrier。
- 預設 execution carrier：`aaa-tools/.github/workflows/v2-1-44-init-orchestration-runtime-baseline.yml` 草案，待 reviewer 批准後才可升格為 Step2 carrier。

## Out of Scope (Step1)
- 不做 readiness adjudication。
- 不宣稱 full execution readiness。
- 不宣稱 orchestration 已 remote-pass。

## Triple-Summary Protocol (v2.1.44)
### 1. Strategic Plan
把 supported path 從文件順序推進到可執行 runtime sequence。

### 2. Schema Evolution
新增 `init-orchestration-runtime-baseline.v0.1.schema.json`，固定 step graph、dependency law、retry/downgrade semantics、execution trace。

### 3. Component Architecture
Step1 只 formalize orchestration contract；Step2 若啟動，才以 remote workflow 驗證 machine-executable init flow。
