# Step1 Plan: v2.1.39 Local Sandbox Bootstrap Profile

- date: 2026-04-05
- version: v2.1.39
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
目前 remote client 常先在 `/tmp` 做 disposable sandbox 驗證，但 AAA 對外仍偏 GitHub/PR/CI bootstrap 敘事，local-only profile 尚未正式化。

## Objective
建立 `v2.1.39` 的 Step1 契約基線，讓 `local_sandbox` 成為正式支持的 bootstrap environment profile。

## Upstream Runtime Laws Consumed
- `v2.1.31`
- `v2.1.37`
- `v2.1.38`

## No Semantics Override
- `local_sandbox` 是 supported bootstrap environment profile。
- `local_sandbox` 不是 user-invented dry-run interpretation。
- `local_sandbox` 不是 dry-run alias。
- 本版不建立新的 package/topology law。
- `local_sandbox` 定義的是 execution environment profile，不是 public supported path count。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-05-v2.1.39-local-sandbox-bootstrap-profile-plan.md`
2. `internal/development/audits/2026-04-05-v2.1.39-local-sandbox-bootstrap-profile-audit.md`
3. `internal/development/reviews/2026-04-05-v2.1.39-local-sandbox-bootstrap-profile-diff-paths.md`
4. `internal/development/reviews/2026-04-05-v2.1.39-step1-exit-checklist.md`
5. `internal/development/contracts/ops/local-sandbox-bootstrap-profile.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-05-v2.1.39-local-sandbox-bootstrap-profile.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-05-v2.1.39-local-sandbox-bootstrap-profile.fail.json`
8. `ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須 machine-checkable 固定 `profile_name=local_sandbox`。
2. `local_sandbox` 不得依賴 GitHub PR creation、branch protection side effects 或 org-level repo creation。
3. schema 必須顯式表達 `dry_run_alias=false`。
4. `local_sandbox` 必須產出 bootstrap report 與 candidate evidence bundle。
5. `version_index.md` 已追加 `v2.1.39` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。

## Out of Scope (Step1)
- 不實作 local sandbox profile runtime。
- 不改寫 dry-run semantics。
- 不處理 evidence promotion。

## Triple-Summary Protocol (v2.1.39)
### 1. Strategic Plan
把 disposable local bootstrap 從 ad hoc dry-run 習慣，提升為正式支持的 environment profile。

### 2. Schema Evolution
新增 `local-sandbox-bootstrap-profile.v0.1.schema.json`，固定 local-only bootstrap profile 契約。

### 3. Component Architecture
Step1 僅交付 local_sandbox profile contract；Step2 若啟動，才對應到 CLI/profile wiring 與 report outputs。

## Appendix Ref
本版只建立 local sandbox profile，不建立新的 bootstrap semantics。
