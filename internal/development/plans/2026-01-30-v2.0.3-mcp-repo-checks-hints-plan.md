<template id="plan">
# Implementation Plan: v2.0.3 MCP Repo-Checks Hints (Phase 2)

## Goal Description
Add MCP-level post-init hints so remote agents that rely on MCP responses (and may not read CLI stdout) still receive the mandatory `aaa init repo-checks --suite governance` instruction after repo creation/initialization. This is Phase 2 only; A2A payload hints remain Phase 3.

## User Review Required
> [!IMPORTANT]
> 1) This plan only implements MCP response hints (Phase 2). A2A payload hints are deferred to Phase 3.
> 2) The hint must be non-blocking and must not alter repo-checks logic or gating.

## Proposed Changes
### [aaa-tools]
#### [MODIFY] MCP tool response payload(s)
- Add a standardized `post_init_required` (or `next_steps`) field containing `aaa init repo-checks --suite governance` to the MCP response for repo creation/initialization flows.
- Ensure MCP JSON remains machine-readable and stable; do not change existing keys.

#### [NEW/MODIFY] MCP message helper
- Centralize MCP hint text/fields to avoid duplication and keep CLI/MCP messaging aligned.

### [aaa-tpl-docs]
#### [NEW] internal/development/audits/2026-01-30-v2.0.3-mcp-repo-checks-hints-audit.md
- Record verification evidence for MCP response payloads and note deferred A2A work.

## Triple-Summary Protocol (v2.0.3)
### 1. Strategic Plan (戰略計畫摘要)
Deliver Phase 2 of remote user AI guidance by emitting repo-checks requirements inside MCP responses so agents relying on MCP (not CLI stdout) still receive mandatory post-init steps.

### 2. Schema Evolution (結構演進摘要)
No schema changes; MCP response payloads gain an additive field (`post_init_required` or `next_steps`).

### 3. Component Architecture (組件架構摘要)
Lightweight MCP response enrichment with a shared helper; no changes to repo-checks execution.

## Verification Plan
### Automated Tests
- If MCP response tests exist: add assertions for presence of the new field.

### Manual Verification
- Invoke the MCP repo-init/create tool and confirm the response includes the required repo-checks instruction.
</template>
