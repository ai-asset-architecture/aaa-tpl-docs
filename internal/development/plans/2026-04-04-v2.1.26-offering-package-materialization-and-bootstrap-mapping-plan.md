# Step1 Plan: v2.1.26 Offering Package Materialization and Bootstrap Mapping

- date: 2026-04-04
- version: v2.1.26
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Current Truth Summary
`v2.1.34` 已建立 topology-aware materialization / bootstrap mapping 的 canonical placement shape；因此 `v2.1.26` 只保留 pre-topology mapping baseline。其責任是保留 package mapping intent，但不得再把 dedicated `.github` repo 當成 universal invariant。

## Objective
建立 `v2.1.26` 的 Step1 契約基線，將 offering package materialization 與 bootstrap mapping 收斂為顯式對映。
本版屬 `offering_package_runtime_enablement_line` 的 pre-topology baseline；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1` 到 `v2.1.25` 的 primary scope；本版只定義 package level 到 repo/template/workflow/governance asset 的對映，不構成 package status 或 completion，且 downstream executable phases 必須以 `v2.1.34` 的 topology-aware mapping 為優先。

## Upstream Runtime Laws Consumed
- `v2.1.24`
- `v2.1.25`
- `v2.1.30`
- `v2.1.32`
- `v2.1.34`

## No Semantics Override
- 本版只處理 pre-topology materialization baseline。
- materialization scope 不得被推定為 governance authority。
- 不得把 dedicated `.github` repo 保留為 universal invariant。
- downstream executable phases 必須消費 `v2.1.34` 的 topology-aware mapping。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.26-offering-package-materialization-and-bootstrap-mapping-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.26-offering-package-materialization-and-bootstrap-mapping-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.26-offering-package-materialization-and-bootstrap-mapping-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.26-step1-exit-checklist.md`
5. `internal/development/contracts/ops/offering-package-materialization-and-bootstrap-mapping.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.26-offering-package-materialization-and-bootstrap-mapping.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.26-offering-package-materialization-and-bootstrap-mapping.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須以 machine-checkable 方式定義 materialization 與 bootstrap mapping，不允許 prose-only fallback。
2. schema 必須明確列出 required repos、template sync、workflow enablement 與 governance asset load list。
3. materialization mapping 必須以既有 resolution shape 為輸入，不得重新解釋 mother draft。
4. 本版必須明寫其為 pre-topology baseline，post-topology executable phases 必須以 `v2.1.34` mapping precedence 為準。
5. 本版不得把 materialization success 視為 package activation、status 或 completion。
6. fail example 必須表達至少一個 required repo 缺漏，以及至少一個 template/workflow/asset 對映不完整卻仍宣稱 mapping 完成的違規情形。
7. 本版必須明確聲明其僅消費既有 contract，不得出現隱性 primary law 增殖。
8. `version_index.md` 已追加 `v2.1.26` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`。
9. `version_index.md` 的 `status` / `run_ref` / step wording 必須使用 canonical phrase，不得出現同義改寫。

## Out of Scope (Step1)
- 本版只建立 Step1 runtime-plane baseline，不實作 runtime entry / validator / CLI wiring / frontend publish / workflow artifact。
- 不建立 package status / package evidence / package activation。
- 不修改 `aaa-tools`、`aaa-docs`、`aaa-tpl-frontend`、`aaa-evals` 或任何 runtime domain code。

## Risks
- 若 mapping 對映不顯式列出 repo/template/workflow/asset，後續 bootstrap materialization 會退回 README 或人工理解。
- 若 materialization success 與 package status 邊界不切開，後續很容易出現「套上去就算完成」的誤判。

## Triple-Summary Protocol (v2.1.26)
### 1. Strategic Plan (戰略計畫摘要)
把 offering package 從「定義有什麼」推進到「要真正建立什麼」，固定 bootstrap materialization 的顯式 mapping。

### 2. Schema Evolution (結構演進摘要)
新增 `offering-package-materialization-and-bootstrap-mapping.v0.1.schema.json`，固定 required repos、template sync、workflow enablement 與 governance asset mapping。

### 3. Component Architecture (組件架構摘要)
Step1 僅交付 materialization mapping bundle 與 pass/fail fixtures；Step2 若啟動，才把此 bundle 對應到 `aaa-tools` package materialization runtime。

## Appendix Ref
本版屬 `offering_package_runtime_enablement_line`。
本版只處理 materialization and bootstrap mapping，不建立新的 primary law，也不得把 materialization success 視為 status/completion。
