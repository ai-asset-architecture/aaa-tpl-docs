# Implementation Plan: v1.2 Semantic Registry & Object-Centric Governance

## Goal Description
AAA v1.2 的核心目標是將 Registry 從單純的「靜態資產列表」升級為具備自我描述能力的「語義化能力目錄」。
透過引入 **Object-Centric Governance** (物件導向治理) 與 **Natural Language Capabilities** (自然語言能力描述)，我們將建立一個讓 AI Agent 能理解、查詢並自動套用治理策略的「資產作業系統」。
此里程碑亦包含嚴格的 **Version Handshake** 機制，確保 CLI 與 Registry Schema 的版本兼容性。

## User Review Required
> [!IMPORTANT]
> **Schema v2 Upgrade**: `registry_index.json` 將升級至 Schema v2.0。這是一個 Breaking Change。
> **Compatibility Strategy**: 將採用 Dual-Publish (v1 & v2) 策略與 `min_cli_version` 檢查，確保舊版客戶端能優雅地失敗或獲得升級提示。

## Proposed Changes

### [aaa-tpl-docs]
#### [MODIFY] [internal/index.json](file:///Users/imac/Documents/Code/AI-Lotto/AAA_WORKSPACE/aaa-tpl-docs/internal/index.json)
- 更新 `milestone` 為 `v1.2` 並初始化相關路徑配置。

#### [NEW] [internal/development/plans/2026-01-28-v1.2-init-plan.md](file:///Users/imac/Documents/Code/AI-Lotto/AAA_WORKSPACE/aaa-tpl-docs/internal/development/plans/2026-01-28-v1.2-init-plan.md)
- 本實作計畫文檔。

#### [NEW] [internal/development/specs/v1.2-registry-schema-v2.md](file:///Users/imac/Documents/Code/AI-Lotto/AAA_WORKSPACE/aaa-tpl-docs/internal/development/specs/v1.2-registry-schema-v2.md)
- 定義 Registry Schema v2.0 的完整規格（含 Object Types 與 Capability Semantics）。

### [aaa-tools] (Upcoming)
#### [NEW] [aaa/schemas/registry_v2.py]
- Pydantic 模型定義，支援 `object_types` 與 `capabilities` 驗證。

#### [MODIFY] [aaa/registry/client.py]
- 實作 Version Handshake 邏輯 (`min_cli_version` check)。
- 新增 `query` 方法支援語義搜索。

## Triple-Summary Protocol (v1.2)

### 1. Strategic Plan (戰略計畫摘要)
AAA v1.2 的核心戰略在於實現「語義化治理」。隨著 AAA 組織規模的擴張，單純的列表式 Registry 已無法滿足 AI Agent 對於資產能力的動態探索需求。本計畫將透過引入 **「自然語言描述 (Natural Language Descriptors)」** 與「能力查詢接口」，將 AAA 轉化為一個具備自我描述能力的「資產作業系統」。這不僅能顯著提升 Agent 在複雜多 Repo 環境下的資產定位效率 (RAG-Ready)，更為後續 v1.5 的自動修復與 v2.0 的 Agent OS 奠定堅實的架構基礎。戰略重點首要在於定義與標準化 `Capability API`，確保與 MCP (Model Context Protocol) 的深度相容性。

### 2. Schema Evolution (結構演進摘要)
在結構層面，v1.2 將迎來 `registry_index.json` 的二次重大升級（v2 Schema）。核心變動包括：
1. **Object-Centric Governance**：從扁平列表轉向以 `object_type` 為核心的繼承模型。初期鎖定四大核心類型：`service` (API治理), `library` (版號治理), `data-pipeline` (Schema治理), `infra-as-code` (安全治理)。
2. **Semantic Capabilities**：`capabilities` 欄位將採用自然語言描述（如 "enforces PEP8 compliance"）以利於向量檢索。
3. **Version Handshake**：引入 `schema_version` 與 `min_cli_version` 欄位，實施「Fail Fast」策略，確保 CLI 版本兼容性。

### 3. Component Architecture (組件架構摘要)
技術組件上，`aaa-tools` 將新增 `registry.query` 子模組，用於解析語義查詢並執行能力匹配邏輯。`ai-asset-architecture-registry` 需更新 CI 流程驗證語義完整性。**遷移策略**上，初期將採用 Dual-Publish 模式（同時發布 `registry_v1.json` 與 `registry_v2.json`），給予舊專案 2-4 週緩衝期。`aaa-tpl-docs` 將新增 v2 Schema 的規格文檔與 `Capability Catalog` 生成腳本。

## Verification Plan

### Automated Tests
- **Schema Validation**: 針對 `registry_index.v2.json` 運行 Pydantic 驗證，確保所有 Object Types 與 Capabilities 符合格式。
- **Version Compatibility**: 模擬舊版 CLI 存取 v2 Registry，驗證是否觸發 "Upgrade Required" 錯誤訊息。

### Manual Verification
- **Semantic Query**: 人工測試 `aaa registry query "find packs for API security"`，確認能否正確回傳 `inherited_packs` 中的安全模組。
