# Step1 Plan: v2.1.19 Runtime Composition Root and System Assembly

- date: 2026-04-04
- version: v2.1.19
- owner: AI Agent
- scope: Step1 only (contracts/governance assets)
- status: FOR_REVIEW

## Objective
建立 `v2.1.19` 的 Step1 契約基線，將 `v2.1.11 ~ v2.1.18` 的 mandatory runtime planes 收斂為單一 composition root。
本版屬 `mandatory core absorption line` closeout version；可引用 shared canonical law，但不得重新定義、覆寫或吸收 `v2.1.1` 到 `v2.1.18` 的 primary scope；本版只定義 system assembly、bootstrap order 與 composition boundary。

## Deliverables (No-Glob)
1. `internal/development/plans/2026-04-04-v2.1.19-runtime-composition-root-and-system-assembly-plan.md`
2. `internal/development/audits/2026-04-04-v2.1.19-runtime-composition-root-and-system-assembly-audit.md`
3. `internal/development/reviews/2026-04-04-v2.1.19-runtime-composition-root-and-system-assembly-diff-paths.md`
4. `internal/development/reviews/2026-04-04-v2.1.19-step1-exit-checklist.md`
5. `internal/development/contracts/ops/runtime-composition-root-and-system-assembly.v0.1.schema.json`
6. `internal/development/contracts/ops/examples/pass/2026-04-04-v2.1.19-runtime-composition-root-and-system-assembly.pass.json`
7. `internal/development/contracts/ops/examples/fail/2026-04-04-v2.1.19-runtime-composition-root-and-system-assembly.fail.json`
8. `aaa-tpl-docs/ops/index/version_index.md`

## Acceptance Criteria
1. schema 必須以 machine-checkable 方式定義 composition root，不允許 prose-only fallback。
2. schema 必須明確定義 bootstrap order、consumed runtime planes、startup boundary 與 assembly outcome。
3. 本版是 `mandatory core absorption line` closeout version，不得新增新的 primary law。
4. composition root 只負責既有 runtime plane 的裝配，不得擴張 command family universe。
5. fail example 必須表達至少一個 runtime plane 缺漏卻仍宣稱組裝完成，或 composition root 反向奪權改寫既有 plane semantics 的違規情形。
6. 本版必須明確聲明其僅消費既有 contract，不得出現隱性 primary law 增殖。
7. `version_index.md` 已追加 `v2.1.19` Step1 row，狀態為 `UNVERIFIED` 且 `run_ref=N/A (step2-pending)`，且 `v2.1.11 ~ v2.1.19` Step1 row wording 必須一致。

## Out of Scope (Step1)
- 本版只建立 Step1 runtime-plane baseline，不實作 runtime entry / validator / CLI wiring / frontend publish / workflow artifact。
- 不啟動 `v2.1.20 ~ v2.1.22` conditional expansion line。
- 不修改 `aaa-tools`、`aaa-tpl-frontend`、`aaa-evals` 或任何 runtime domain code。

## Risks
- 若 composition root 未被限制為 closeout assembly，它很容易退化成新的總控 law。
- 若 bootstrap order 不先固定，後續主線 closeout 無法被穩定驗證。

## Triple-Summary Protocol (v2.1.19)
### 1. Strategic Plan (戰略計畫摘要)
把 `v2.1.11 ~ v2.1.18` 的 mandatory runtime planes 從平行存在，提升為可共同啟動、共同 closeout 的完整系統。

### 2. Schema Evolution (結構演進摘要)
新增 `runtime-composition-root-and-system-assembly.v0.1.schema.json`，固定 bootstrap order、consumed planes 與 assembly boundary。

### 3. Component Architecture (組件架構摘要)
Step1 僅交付 composition root bundle 與 pass/fail fixtures；Step2 若啟動，才把此 bundle 對應到 `aaa-tools` runtime composition root。

## Appendix Ref
本版屬 `mandatory core absorption line` closeout version。
本版只處理系統裝配，不建立新的 primary law。
