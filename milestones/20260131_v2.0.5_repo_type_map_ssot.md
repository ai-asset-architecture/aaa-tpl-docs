# v2.0.5 Milestone: Repo Type Map SSOT + Soft Enum (2026-01-31)

> **Status**: ✅ **COMPLETED**
> **Goal**: Centralize repo_type mapping as SSOT, add missing-mapping fail-fast, and emit soft-enum WARN in nightly without breaking consumers.

## Executive Summary
**ZH-TW:** v2.0.5 將 nightly 的 repo_type 字典集中到 `repo_type_map.json`，並加入缺失 mapping 的 fail-fast 與 unknown repo_type 的 soft WARN。此版本以「nightly 導向的可觀測性」為主，不更動 schema enum，避免破壞面擴大，同時完成審計證據與資產登錄。

**EN:** v2.0.5 centralizes the nightly repo_type map into `repo_type_map.json`, adds fail-fast for missing mappings, and emits soft-enum WARNs for unknown repo_type values. The scope stays in nightly (no schema enum changes), with audit evidence and asset registration completed.

## Key Deliverables
1. **SSOT Repo Type Map**
   - `aaa-actions/.aaa/repo_type_map.json` as the single source of truth.
2. **Nightly Enforcement + Observability**
   - Missing mapping fail-fast + WARN unknown repo_type tokens.
3. **Validation Evidence**
   - Happy path + edge/negative injections recorded in audit.
4. **Asset Registration**
   - v2.0.5 audit + SSOT map registered in registry.

## Artifacts
- [Completion Report](../internal/development/milestones/completion-reports/aaa_v2.0.5_completion_report_20260131.md)
- [Audit Evidence](../internal/development/audits/2026-01-31-v2.0.5-repo-type-map-audit.md)
- [Repo Type Map SSOT](../../aaa-actions/.aaa/repo_type_map.json)
- [Nightly Workflow](../../aaa-actions/.github/workflows/nightly-governance.yaml)
