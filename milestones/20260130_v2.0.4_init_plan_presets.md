# v2.0.4 Milestone: Init Plan Presets (2026-01-30)

> **Status**: ✅ **COMPLETED**
> **Goal**: Introduce v2.0 init plan presets with default Governance-Native behavior and explicit `.github` inclusion, while retaining legacy compatibility.

## Executive Summary
**ZH-TW:** v2.0.4 將 init 設計升級為「單檔 presets + default_preset」，預設 Governance-Native 並強制包含 `.github`，同時保留 legacy `repos` 相容流程。CLI 新增 `--preset` 覆寫能力與明確的 legacy/mixed mode 可觀測 token，並完成測試與審計證據。

**EN:** v2.0.4 upgrades init to a single plan with `presets + default_preset`, defaulting to Governance-Native and enforcing `.github`, while retaining legacy `repos` compatibility. CLI adds `--preset` override and observable legacy/mixed-mode tokens, with tests and audit evidence completed.

## Key Deliverables
1. **Plan Presets (v2.0)**
   - Single plan file with `governance_native` and `lean` presets.
2. **Preset Resolver + CLI Override**
   - Resolver order `--preset > default_preset > legacy` with fail-fast rules and MODE/WARN tokens.
3. **Schema + Tests + Docs**
   - Schema expanded for presets; critical tests added; bootstrap/CLI guidance updated.
4. **Asset Registration**
   - v2.0.4 assets registered in registry with explicit unchanged declarations.

## Artifacts
- [Completion Report](../internal/development/milestones/completion-reports/aaa_v2.0.4_completion_report_20260130.md)
- [Audit Evidence](../internal/development/audits/2026-01-30-v2.0.4-audit-presets.md)
- [Plan File](../../aaa-tools/runbooks/init/plan.v2.0.json)
